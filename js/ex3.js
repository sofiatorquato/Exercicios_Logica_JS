//Calcular a área de um retângulo (Base * Altura) e exibir o resultado.

let b = parseFloat(prompt("Informe a base do retêngulo em metros: "));
let a = parseFloat(prompt("Informe a altura do retângulo me metros: "));
let = area = b*a;

alert ("A área do retângulo é de "+ area.toFixed(2) + "m²");